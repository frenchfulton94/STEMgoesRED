# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'STEM goes RED' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  # Pods for STEM goes RED

  target 'STEM goes REDTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'STEM goes REDUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
